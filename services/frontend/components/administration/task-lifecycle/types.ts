// Shared types for the Task Lifecycle dashboard.
// Ported VERBATIM from OLD
// app/routes/administration/project-management/task-lifecycle.tsx.

export type Project = { _id: string; title: string };
export type Task = { _id: string; title: string };
export type Workspace = { _id: string; name: string };
