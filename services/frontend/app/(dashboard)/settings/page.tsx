// Settings page (Next.js App Router port of the OLD app/routes/settings/settings.tsx).
// In the OLD app this route is a bare placeholder that renders only the literal
// text "Settings". UI is ported verbatim so the screen is pixel-identical.

import React from 'react';

export default function SettingsPage() {
  return (
    <div>
      Settings
    </div>
  );
}
