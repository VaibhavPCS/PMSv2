// Archived page (Next.js App Router port of the OLD
// app/routes/archived/archived.tsx). Thin wrapper around the presentational
// ArchivedView; UI is pixel-identical to the old route.

import { ArchivedView } from '@/components/archived/ArchivedView';

export default function ArchivedPage() {
  return <ArchivedView />;
}
